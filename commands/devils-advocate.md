---
description: Attack a decision before acting on it. One cold adversary, ranked attacks, a probability you are wrong, a forced verdict.
---

Run the `devils-advocate` skill on this decision: $ARGUMENTS

If `$ARGUMENTS` is empty, ask for the decision in one line, then run the skill. Follow the skill's workflow exactly: claim block, cold Advocate dispatch, run every check that is a command, label every attack, one-word verdict. Show the Advocate's output verbatim.
